# Jangan set VITE_API_URL di sini — gunakan file mode agar dev/prod terpisah:
#   .env.development  → npm run dev (localhost API)
#   .env.production   → npm run build (Hostinger / produksi)
# Override per mesin (tidak di-commit): .env.local atau .env.development.local
#
# Lihat frontend/.env.example
VITE_GA_ID=G-7L1XYT9JJP
