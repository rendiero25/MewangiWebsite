# Salin ke .env.development / .env.production atau .env.local sesuai kebutuhan.
# Dev (Vite):   http://localhost:3000/api  — backend harus jalan di PORT tersebut
# Prod build:   https://domain-anda/api
VITE_API_URL=
